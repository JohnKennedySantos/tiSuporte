<header>
  <nav class="nav-container">

  
    <div class="mobile-menu-area">
      <div class="mobile-menu" id="mobile-menu">
        <div class="line1"></div>
        <div class="line2"></div>
        <div class="line3"></div>
      </div>
      <span class="page-title">{{ titulo }}</span>
       <span class="page-title"></span> 
    </div>

    <div class="logo-area">
      <a class="logo" href="/">
        <img src="/images/LogoPadrao.png" alt="Logo Suporte TI" />
      </a>
    </div>

   <div class="user-area">
      {% if usuario %}
        <span class="user-name">Olá, {{ usuario.Usuario }}</span>
      {% else %}
        <span class="user-name">Não logado</span>
      {% endif %}
    </div>
  </nav>
</header>
